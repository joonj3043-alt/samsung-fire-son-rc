(()=>{'use strict';const form=document.querySelector('#consult-form');if(!form)return;const cfg=window.SITE_CONFIG||{},status=form.querySelector('.form-status'),topic=new URLSearchParams(location.search).get('topic');if(topic){const opt=[...form.topic.options].find(o=>o.text===topic);if(opt)form.topic.value=opt.value}form.addEventListener('submit',async e=>{e.preventDefault();status.textContent='';if(!form.checkValidity()){form.reportValidity();status.textContent='필수 항목과 개인정보 동의를 확인해 주세요.';return}if(form.website.value)return;if(!cfg.formEndpoint){status.textContent='상담폼 연결 설정이 아직 완료되지 않았습니다. 전화 상담을 이용해 주세요.';return}const button=form.querySelector('button[type="submit"]');button.disabled=true;button.textContent='전송 중…';try{const data=new FormData(form);const response=await fetch(cfg.formEndpoint,{method:'POST',body:data,headers:{Accept:'application/json'}});if(!response.ok)throw new Error('send failed');form.reset();status.textContent='상담 신청이 접수되었습니다. 확인 후 연락드리겠습니다.'}catch(err){status.textContent='전송하지 못했습니다. 잠시 후 다시 시도하거나 전화로 문의해 주세요.'}finally{button.disabled=false;button.textContent='상담 신청하기'}})})();
